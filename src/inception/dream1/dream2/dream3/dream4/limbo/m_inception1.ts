/* eslint-disable */
// We need to go deeper. Each level of nesting is another dream layer.
${Array.from({length: 4200}, (_, i) => `const dream${i} = "m";`).join('\n')}
export const inception1 = "m";
